Visual Studio Code 접속
Open Folder로 ansimcall 열기
Terminal -> new Terminal

(라이브러리 설치) 
pip install -r requirements.txt

(KoBERT 학습) 
python train_multilabel_kobert.py --csv kobert_multilabel_training.csv --output models/kobert_multilabel

(프로그램 실행) 
streamlit run app.py